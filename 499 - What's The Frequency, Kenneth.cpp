#include<bits/stdc++.h>

using namespace std;

int main()
{
    char ch[505];

    int max,int ar[505];

    while(gets(ch))
    {
        getchar();

        int len=strlen(ch);
        for(int i=0;i<len;i++)
        {

        }
    }

    return 0;
}
