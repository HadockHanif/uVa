#include<bits/stdc++.h>

using namespace std;

int main()
{

    long a,b,c,d,e,f,g,h,i;

    while(scanf("%ld %ld %ld %ld %ld %ld %ld %ld %ld",&a,&b,&c,&d,&e,&f,&g,&h,&i)==9)
    {
                if(a == -1 )
                {
                    cout<<"-x^8 ";
                }
                else if(a == 1)
                {
                    cout<<"x^8 ";
                }
                else
                {
                    cout<<a<<"x^8";
                }

                if(a == 0)
                {

                }
    }

    return 0;
}
